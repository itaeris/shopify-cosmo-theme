<section
  id="AerisHero-{{ section.id }}"
  class="aeris-hero"
  data-autoplay="{{ section.settings.autoplay }}"
  data-speed="{{ section.settings.autoplay_speed | times: 1000 }}"
>
  <div class="aeris-hero__slider">
    {% for block in section.blocks %}
      <div
        class="aeris-hero__slide {% if forloop.first %}is-active{% endif %}"
        data-slide="{{ forloop.index0 }}"
        {{ block.shopify_attributes }}
      >
        {% if block.settings.background_image != blank %}
          <div class="aeris-hero__background">
            {{
              block.settings.background_image
              | image_url: width: 2400
              | image_tag:
                loading: 'lazy',
                widths: '750, 1100, 1500, 2000, 2400',
                sizes: '100vw',
                class: 'aeris-hero__background-image'
            }}
          </div>
        {% else %}
          <div class="aeris-hero__background aeris-hero__background--placeholder">
            {{ 'lifestyle-1' | placeholder_svg_tag }}
          </div>
        {% endif %}

        <div
          class="aeris-hero__overlay"
          style="opacity: {{ section.settings.overlay_opacity | divided_by: 100.0 }};"
        ></div>

        {% if block.settings.show_small_image and block.settings.small_image != blank %}
          <div class="aeris-hero__small-card">
            {{
              block.settings.small_image
              | image_url: width: 1000
              | image_tag:
                loading: 'lazy',
                widths: '300, 500, 700, 1000',
                sizes: '(max-width: 749px) 140px, 300px',
                class: 'aeris-hero__small-image'
            }}
          </div>
        {% endif %}

        <div class="aeris-hero__content">
          {% if block.settings.heading != blank %}
            <h2 class="aeris-hero__heading">
              {{ block.settings.heading }}
            </h2>
          {% endif %}

          {% if block.settings.subtitle != blank %}
            <div class="aeris-hero__subtitle">
              {{ block.settings.subtitle }}
            </div>
          {% endif %}

          {% if block.settings.button_text != blank %}
            <a
              href="{{ block.settings.button_link | default: '#' }}"
              class="aeris-hero__button"
            >
              {{ block.settings.button_text }}
            </a>
          {% endif %}
        </div>
      </div>
    {% endfor %}
  </div>

  {% if section.blocks.size > 1 %}
    {% if section.settings.show_arrows %}
      <button
        class="aeris-hero__arrow aeris-hero__arrow--prev"
        type="button"
        aria-label="Previous slide"
      >
        ‹
      </button>

      <button
        class="aeris-hero__arrow aeris-hero__arrow--next"
        type="button"
        aria-label="Next slide"
      >
        ›
      </button>
    {% endif %}

    {% if section.settings.show_dots %}
      <div class="aeris-hero__dots">
        {% for block in section.blocks %}
          <button
            type="button"
            class="aeris-hero__dot {% if forloop.first %}is-active{% endif %}"
            data-dot="{{ forloop.index0 }}"
            aria-label="Go to slide {{ forloop.index }}"
          ></button>
        {% endfor %}
      </div>
    {% endif %}
  {% endif %}
</section>

<style>
  #AerisHero-{{ section.id }} {
    position: relative;
    width: 100%;
    overflow: hidden;
    background: #111;
  }

  #AerisHero-{{ section.id }} .aeris-hero__slider {
    position: relative;
    width: 100%;
    min-height: {{ section.settings.desktop_height }}px;
  }

  #AerisHero-{{ section.id }} .aeris-hero__slide {
    position: absolute;
    inset: 0;
    width: 100%;
    min-height: {{ section.settings.desktop_height }}px;

    opacity: 0;
    visibility: hidden;
    pointer-events: none;

    transition:
      opacity 0.7s ease,
      visibility 0.7s ease;
  }

  #AerisHero-{{ section.id }} .aeris-hero__slide.is-active {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
    z-index: 2;
  }

  #AerisHero-{{ section.id }} .aeris-hero__background {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
  }

  #AerisHero-{{ section.id }} .aeris-hero__background-image,
  #AerisHero-{{ section.id }} .aeris-hero__background--placeholder svg {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  #AerisHero-{{ section.id }} .aeris-hero__background--placeholder {
    background: #e5e5e5;
  }

  #AerisHero-{{ section.id }} .aeris-hero__overlay {
    position: absolute;
    inset: 0;
    z-index: 1;

    background: #000;

    pointer-events: none;
  }

  #AerisHero-{{ section.id }} .aeris-hero__small-card {
    position: absolute;
    z-index: 4;

    left: {{ section.settings.small_image_left }}px;
    bottom: {{ section.settings.small_image_bottom }}px;

    width: {{ section.settings.small_image_width }}px;
    max-width: calc(100vw - 48px);

    overflow: hidden;

    background: #f2f2f2;
    border: {{ section.settings.small_image_border_width }}px solid
      rgba(0, 0, 0, {{ section.settings.small_image_border_opacity | divided_by: 100.0 }});

    border-radius: {{ section.settings.small_image_radius }}px;

    box-shadow:
      0 10px 35px rgba(0, 0, 0, 0.12);
  }

  #AerisHero-{{ section.id }} .aeris-hero__small-image {
    display: block;
    width: 100%;
    height: auto;

    aspect-ratio: {{ section.settings.small_image_ratio }};

    object-fit: cover;
  }

  #AerisHero-{{ section.id }} .aeris-hero__content {
    position: absolute;
    z-index: 4;

    right: {{ section.settings.content_right }}%;
    bottom: {{ section.settings.content_bottom }}%;

    width: min(500px, 42vw);

    color: #fff;
    text-align: left;
  }

  #AerisHero-{{ section.id }} .aeris-hero__heading {
    margin: 0;

    color: #fff;

    font-size: clamp(
      {{ section.settings.heading_min_size }}px,
      {{ section.settings.heading_desktop_size | divided_by: 14.0 }}vw,
      {{ section.settings.heading_desktop_size }}px
    );

    line-height: 0.95;
    font-weight: {{ section.settings.heading_weight }};
    letter-spacing: -0.03em;
  }

  #AerisHero-{{ section.id }} .aeris-hero__subtitle {
    margin-top: 10px;

    color: #fff;

    font-family: Georgia, "Times New Roman", serif;
    font-size: clamp(20px, 2vw, 32px);
    line-height: 1.2;
    font-style: italic;
  }

  #AerisHero-{{ section.id }} .aeris-hero__subtitle p {
    margin: 0;
  }

  #AerisHero-{{ section.id }} .aeris-hero__button {
  display: inline-flex;
  align-items: center;
  justify-content: center;

  margin-top: 22px;
  padding: 10px 26px;

  min-width: 220px;

  color: #ffffff;
  background: transparent;

  border: 1.5px solid #ffffff;
  border-radius: 5px;

  font-family: Georgia, "Times New Roman", serif;
  font-size: 18px;
  line-height: 1.2;
  font-style: normal;
  font-weight: 400;
  letter-spacing: 0.02em;

  text-decoration: none;

  transition:
    background 0.25s ease,
    color 0.25s ease,
    transform 0.25s ease;
}

  #AerisHero-{{ section.id }} .aeris-hero__arrow {
    position: absolute;
    z-index: 10;
    top: 50%;

    display: flex;
    align-items: center;
    justify-content: center;

    width: 44px;
    height: 44px;

    padding: 0;
    border: 1px solid rgba(255, 255, 255, 0.5);
    border-radius: 50%;

    background: rgba(0, 0, 0, 0.25);
    color: #fff;

    font-family: Arial, sans-serif;
    font-size: 34px;
    line-height: 1;

    cursor: pointer;

    transform: translateY(-50%);

    transition:
      background 0.2s ease,
      border-color 0.2s ease;
  }

  #AerisHero-{{ section.id }} .aeris-hero__arrow:hover {
    background: rgba(0, 0, 0, 0.55);
    border-color: rgba(255, 255, 255, 0.9);
  }

  #AerisHero-{{ section.id }} .aeris-hero__arrow--prev {
    left: 20px;
  }

  #AerisHero-{{ section.id }} .aeris-hero__arrow--next {
    right: 20px;
  }

  #AerisHero-{{ section.id }} .aeris-hero__dots {
    position: absolute;
    z-index: 10;

    left: 50%;
    bottom: 18px;

    display: flex;
    align-items: center;
    justify-content: center;

    gap: 10px;

    transform: translateX(-50%);
  }

  #AerisHero-{{ section.id }} .aeris-hero__dot {
    width: 9px;
    height: 9px;

    padding: 0;

    border: 1px solid rgba(255, 255, 255, 0.9);
    border-radius: 50%;

    background: transparent;

    cursor: pointer;

    transition:
      background 0.2s ease,
      transform 0.2s ease;
  }

  #AerisHero-{{ section.id }} .aeris-hero__dot.is-active {
    background: #fff;
    transform: scale(1.15);
  }

  @media screen and (max-width: 989px) {
    #AerisHero-{{ section.id }} .aeris-hero__slider,
    #AerisHero-{{ section.id }} .aeris-hero__slide {
      min-height: {{ section.settings.tablet_height }}px;
    }

    #AerisHero-{{ section.id }} .aeris-hero__small-card {
      left: 24px;
      bottom: 24px;

      width: min({{ section.settings.small_image_width }}px, 230px);
    }

    #AerisHero-{{ section.id }} .aeris-hero__content {
      right: 5%;
      bottom: 8%;

      width: min(420px, 48vw);
    }
  }

  @media screen and (max-width: 749px) {
    #AerisHero-{{ section.id }} .aeris-hero__slider,
    #AerisHero-{{ section.id }} .aeris-hero__slide {
      min-height: {{ section.settings.mobile_height }}px;
    }

    #AerisHero-{{ section.id }} .aeris-hero__background-image {
      object-position: center center;
    }

    #AerisHero-{{ section.id }} .aeris-hero__small-card {
      left: {{ section.settings.mobile_small_image_left }}px;
      bottom: {{ section.settings.mobile_small_image_bottom }}px;

      width: {{ section.settings.mobile_small_image_width }}px;
      max-width: calc(100vw - 32px);
    }

    #AerisHero-{{ section.id }} .aeris-hero__content {
      left: 22px;
      right: 22px;
      bottom: 36px;

      width: auto;
      max-width: none;
    }

    #AerisHero-{{ section.id }} .aeris-hero__heading {
      font-size: {{ section.settings.mobile_heading_size }}px;
      line-height: 0.98;
    }

    #AerisHero-{{ section.id }} .aeris-hero__subtitle {
      margin-top: 8px;

      font-size: 21px;
    }

    #AerisHero-{{ section.id }} .aeris-hero__button {
      margin-top: 16px;
      padding: 9px 20px;

      min-width: 145px;

      font-size: 17px;
    }

    #AerisHero-{{ section.id }} .aeris-hero__arrow {
      width: 38px;
      height: 38px;

      font-size: 28px;
    }

    #AerisHero-{{ section.id }} .aeris-hero__arrow--prev {
      left: 10px;
    }

    #AerisHero-{{ section.id }} .aeris-hero__arrow--next {
      right: 10px;
    }

    #AerisHero-{{ section.id }} .aeris-hero__dots {
      bottom: 12px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    #AerisHero-{{ section.id }} .aeris-hero__slide,
    #AerisHero-{{ section.id }} .aeris-hero__button,
    #AerisHero-{{ section.id }} .aeris-hero__arrow,
    #AerisHero-{{ section.id }} .aeris-hero__dot {
      transition: none;
    }
  }
</style>

<script>
  (() => {
    const initAerisHero = () => {
      const section = document.getElementById('AerisHero-{{ section.id }}');

      if (!section || section.dataset.initialized === 'true') return;

      section.dataset.initialized = 'true';

      const slides = Array.from(
        section.querySelectorAll('.aeris-hero__slide')
      );

      const dots = Array.from(
        section.querySelectorAll('.aeris-hero__dot')
      );

      const nextButton = section.querySelector(
        '.aeris-hero__arrow--next'
      );

      const prevButton = section.querySelector(
        '.aeris-hero__arrow--prev'
      );

      if (slides.length === 0) return;

      let currentIndex = 0;
      let autoplayTimer = null;

      const autoplay = section.dataset.autoplay === 'true';
      const speed = Number(section.dataset.speed) || 5000;

      const showSlide = (index) => {
        currentIndex =
          (index + slides.length) % slides.length;

        slides.forEach((slide, slideIndex) => {
          slide.classList.toggle(
            'is-active',
            slideIndex === currentIndex
          );
        });

        dots.forEach((dot, dotIndex) => {
          dot.classList.toggle(
            'is-active',
            dotIndex === currentIndex
          );
        });
      };

      const nextSlide = () => {
        showSlide(currentIndex + 1);
      };

      const previousSlide = () => {
        showSlide(currentIndex - 1);
      };

      const stopAutoplay = () => {
        if (autoplayTimer) {
          clearInterval(autoplayTimer);
          autoplayTimer = null;
        }
      };

      const startAutoplay = () => {
        stopAutoplay();

        if (
          autoplay &&
          slides.length > 1 &&
          !window.matchMedia('(prefers-reduced-motion: reduce)').matches
        ) {
          autoplayTimer = setInterval(
            nextSlide,
            speed
          );
        }
      };

      if (nextButton) {
        nextButton.addEventListener('click', () => {
          nextSlide();
          startAutoplay();
        });
      }

      if (prevButton) {
        prevButton.addEventListener('click', () => {
          previousSlide();
          startAutoplay();
        });
      }

      dots.forEach((dot, index) => {
        dot.addEventListener('click', () => {
          showSlide(index);
          startAutoplay();
        });
      });

      section.addEventListener(
        'mouseenter',
        stopAutoplay
      );

      section.addEventListener(
        'mouseleave',
        startAutoplay
      );

      section.addEventListener(
        'focusin',
        stopAutoplay
      );

      section.addEventListener(
        'focusout',
        startAutoplay
      );

      let touchStartX = 0;
      let touchEndX = 0;

      section.addEventListener(
        'touchstart',
        (event) => {
          touchStartX =
            event.changedTouches[0].screenX;
        },
        { passive: true }
      );

      section.addEventListener(
        'touchend',
        (event) => {
          touchEndX =
            event.changedTouches[0].screenX;

          const difference =
            touchStartX - touchEndX;

          if (Math.abs(difference) < 50) return;

          if (difference > 0) {
            nextSlide();
          } else {
            previousSlide();
          }

          startAutoplay();
        },
        { passive: true }
      );

      showSlide(0);
      startAutoplay();
    };

    if (document.readyState === 'loading') {
      document.addEventListener(
        'DOMContentLoaded',
        initAerisHero
      );
    } else {
      initAerisHero();
    }

    document.addEventListener(
      'shopify:section:load',
      initAerisHero
    );
  })();
</script>

{% schema %}
{
  "name": "Aeris Campaign Slider",
  "tag": "section",
  "class": "section-aeris-campaign-slider",
  "max_blocks": 8,

  "settings": [
    {
      "type": "header",
      "content": "Slideshow"
    },
    {
      "type": "checkbox",
      "id": "autoplay",
      "label": "Autoplay slideshow",
      "default": true
    },
    {
      "type": "range",
      "id": "autoplay_speed",
      "label": "Change slide every",
      "min": 3,
      "max": 10,
      "step": 1,
      "unit": "s",
      "default": 5
    },
    {
      "type": "checkbox",
      "id": "show_arrows",
      "label": "Show arrows",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_dots",
      "label": "Show slide dots",
      "default": true
    },

    {
      "type": "header",
      "content": "Hero size"
    },
    {
      "type": "range",
      "id": "desktop_height",
      "label": "Desktop height",
      "min": 450,
      "max": 900,
      "step": 10,
      "unit": "px",
      "default": 680
    },
    {
      "type": "range",
      "id": "tablet_height",
      "label": "Tablet height",
      "min": 450,
      "max": 800,
      "step": 10,
      "unit": "px",
      "default": 600
    },
    {
      "type": "range",
      "id": "mobile_height",
      "label": "Mobile height",
      "min": 450,
      "max": 800,
      "step": 10,
      "unit": "px",
      "default": 650
    },

    {
      "type": "header",
      "content": "Background"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "label": "Background overlay",
      "min": 0,
      "max": 60,
      "step": 5,
      "unit": "%",
      "default": 10
    },

    {
      "type": "header",
      "content": "Small image - Desktop"
    },
    {
      "type": "range",
      "id": "small_image_width",
      "label": "Small image width",
      "min": 180,
      "max": 450,
      "step": 10,
      "unit": "px",
      "default": 300
    },
    {
      "type": "range",
      "id": "small_image_left",
      "label": "Distance from left",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 24
    },
    {
      "type": "range",
      "id": "small_image_bottom",
      "label": "Distance from bottom",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 24
    },
    {
      "type": "select",
      "id": "small_image_ratio",
      "label": "Small image ratio",
      "default": "4 / 3",
      "options": [
        {
          "value": "1 / 1",
          "label": "Square"
        },
        {
          "value": "4 / 3",
          "label": "Landscape 4:3"
        },
        {
          "value": "3 / 2",
          "label": "Landscape 3:2"
        },
        {
          "value": "16 / 9",
          "label": "Landscape 16:9"
        },
        {
          "value": "4 / 5",
          "label": "Portrait 4:5"
        }
      ]
    },
    {
      "type": "range",
      "id": "small_image_radius",
      "label": "Corner radius",
      "min": 0,
      "max": 40,
      "step": 2,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "small_image_border_width",
      "label": "Border width",
      "min": 0,
      "max": 5,
      "step": 1,
      "unit": "px",
      "default": 1
    },
    {
      "type": "range",
      "id": "small_image_border_opacity",
      "label": "Border opacity",
      "min": 0,
      "max": 100,
      "step": 10,
      "unit": "%",
      "default": 40
    },

    {
      "type": "header",
      "content": "Small image - Mobile"
    },
    {
      "type": "range",
      "id": "mobile_small_image_width",
      "label": "Mobile small image width",
      "min": 100,
      "max": 240,
      "step": 10,
      "unit": "px",
      "default": 140
    },
    {
      "type": "range",
      "id": "mobile_small_image_left",
      "label": "Mobile distance from left",
      "min": 0,
      "max": 60,
      "step": 2,
      "unit": "px",
      "default": 18
    },
    {
      "type": "range",
      "id": "mobile_small_image_bottom",
      "label": "Mobile distance from bottom",
      "min": 100,
      "max": 350,
      "step": 10,
      "unit": "px",
      "default": 190
    },

    {
      "type": "header",
      "content": "Text position"
    },
    {
      "type": "range",
      "id": "content_right",
      "label": "Distance from right",
      "min": 2,
      "max": 20,
      "step": 1,
      "unit": "%",
      "default": 7
    },
    {
      "type": "range",
      "id": "content_bottom",
      "label": "Distance from bottom",
      "min": 2,
      "max": 25,
      "step": 1,
      "unit": "%",
      "default": 8
    },

    {
      "type": "header",
      "content": "Heading"
    },
    {
      "type": "range",
      "id": "heading_desktop_size",
      "label": "Desktop heading size",
      "min": 40,
      "max": 110,
      "step": 2,
      "unit": "px",
      "default": 72
    },
    {
      "type": "range",
      "id": "heading_min_size",
      "label": "Minimum responsive size",
      "min": 28,
      "max": 60,
      "step": 2,
      "unit": "px",
      "default": 46
    },
    {
      "type": "range",
      "id": "mobile_heading_size",
      "label": "Mobile heading size",
      "min": 28,
      "max": 70,
      "step": 2,
      "unit": "px",
      "default": 46
    },
    {
      "type": "select",
      "id": "heading_weight",
      "label": "Heading weight",
      "default": "500",
      "options": [
        {
          "value": "300",
          "label": "Light"
        },
        {
          "value": "400",
          "label": "Regular"
        },
        {
          "value": "500",
          "label": "Medium"
        },
        {
          "value": "600",
          "label": "Semi bold"
        },
        {
          "value": "700",
          "label": "Bold"
        }
      ]
    }
  ],

  "blocks": [
    {
      "type": "slide",
      "name": "Slide",
      "settings": [
        {
          "type": "header",
          "content": "Background"
        },
        {
          "type": "image_picker",
          "id": "background_image",
          "label": "Background image"
        },

        {
          "type": "header",
          "content": "Small image"
        },
        {
          "type": "checkbox",
          "id": "show_small_image",
          "label": "Show small image",
          "default": false
        },
        {
          "type": "image_picker",
          "id": "small_image",
          "label": "Small image"
        },

        {
          "type": "header",
          "content": "Content"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "MERLOT"
        },
        {
          "type": "richtext",
          "id": "subtitle",
          "label": "Subtitle",
          "default": "<p>Unlock Your True Power</p>"
        },
        {
          "type": "text",
          "id": "button_text",
          "label": "Button text",
          "default": "Discover now"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Button link"
        }
      ]
    }
  ],

  "presets": [
    {
      "name": "Aeris Campaign Slider",
      "blocks": [
        {
          "type": "slide"
        },
        {
          "type": "slide"
        }
      ]
    }
  ]
}
{% endschema %}