{%- liquid
  assign aeris_menu_url = menu_link.url | default: '#'
  assign aeris_menu_handle = menu_link.title | handleize
  if aeris_menu_handle == 'shop-all' or aeris_menu_handle == 'shopall'
    assign aeris_menu_url = '/collections/all-products'
  endif
  if aeris_menu_handle == 'new-arrival' or aeris_menu_handle == 'new-arrivals'
    assign aeris_menu_url = '/collections/new-in'
  endif
  if aeris_menu_handle == 'best-seller' or aeris_menu_handle == 'best-sellers'
    assign aeris_menu_url = '/collections/best-sellers-1'
  endif
  if aeris_menu_handle == 'brushes-tools' or aeris_menu_handle == 'brushes-and-tools'
    assign aeris_menu_url = '/collections/accessories'
  endif
  if aeris_menu_handle == 'brush-cleaner' or aeris_menu_handle == 'brush-cleanser'
    assign aeris_menu_url = '/collections/cleansers'
  endif
  if aeris_menu_handle == 'hair-tools'
    assign aeris_menu_url = '/collections/hair-tools'
  endif
  if aeris_menu_handle == 'lifestyle-bags'
    assign aeris_menu_url = '/collections/wellness-tools'
  endif
  if aeris_menu_handle == 'bundles'
    assign aeris_menu_url = '/collections/gift-sets'
  endif
  if aeris_menu_handle == 'our-story' or aeris_menu_handle == 'ourstory'
    assign aeris_menu_url = '/pages/about-us'
  endif
  if aeris_menu_handle == 'track-your-order' or aeris_menu_handle == 'track-order'
    assign aeris_menu_url = '/pages/track-your-order'
  endif
  if aeris_menu_handle == 'contact-us' or aeris_menu_handle == 'contact' or aeris_menu_handle == 'contactus'
    assign aeris_menu_url = '/pages/contact-us'
  endif
  if aeris_menu_handle == 'store-locations' or aeris_menu_handle == 'store-location'
    assign aeris_menu_url = '/pages/store-location'
  endif
  if aeris_menu_handle == 'surabaya'
    assign aeris_menu_url = '/pages/store-location#pakuwon'
  endif
  if aeris_menu_handle == 'bandung'
    assign aeris_menu_url = '/pages/store-location#paskal'
  endif
  if aeris_menu_handle == 'bali'
    assign aeris_menu_url = '/pages/store-location#living-world'
  endif
  if aeris_menu_handle == 'makassar'
    assign aeris_menu_url = '/pages/store-location#trans-studio'
  endif
  if aeris_menu_handle == 'semarang'
    assign aeris_menu_url = '/pages/store-location#dp-mall'
  endif
  if aeris_menu_handle == 'personalization' or aeris_menu_handle == 'personalisation'
    assign aeris_menu_url = '/collections/all-products?view=personalisation'
  endif
-%}
{{- aeris_menu_url -}}
